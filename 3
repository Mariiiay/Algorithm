def fibonacci(n):
    if n <= 1:
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)

//Количество вызовов растет очень быстро. Например, для n=10 — уже 100+ вызовов.
Алгоритм решает ту же задачу много раз.//
