def find_element(arr, target):
    for i in range(len(arr)):
        if arr[i] == target:
            return i
    return -1

//Чтобы найти элемент, возможно, придется проверить каждый.
Чем больше список — тем больше шагов.//
